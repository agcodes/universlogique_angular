export default class PointSet {
	constructor() {
		this.points = [];
	}

	// Méthode pour ajouter un vecteur au PointSet
	addPoint(vector) {
		this.points.push(vector);
	}

	// Autres méthodes utiles en fonction de vos besoins
}