import { Routes } from '@angular/router';
import { CanvasDrawingComponent } from './canvas-drawing/canvas-drawing.component';

export const ADDON_CANVAS_ROUTES: Routes = [{ path: '', component: CanvasDrawingComponent }];
